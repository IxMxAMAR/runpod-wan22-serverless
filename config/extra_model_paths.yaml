# Network volume model paths for RunPod serverless
# Mounts at /runpod-volume/ — contains LoRAs and RIFE models
# Base models (UNETs, VAE, text encoders) are baked into the Docker image

runpod_network_volume:
    base_path: /runpod-volume/
    loras: models/loras/
